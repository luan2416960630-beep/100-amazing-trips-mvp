# 飞猪「100种不可思议旅行」MVP 产品需求文档

---

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 100种不可思议旅行 MVP 产品需求文档 |
| 产品名称 | 100种不可思议旅行 |
| 所属品牌 | 飞猪旅行 |
| 文档版本 | v1.0 |
| 创建日期 | 2026-06-04 |
| 文档状态 | 初稿 |
| 作者 | 产品团队 |

**变更记录**

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| v1.0 | 2026-06-04 | 初稿创建，定义MVP范围 | 产品团队 |

---

## 2. 产品概述

### 2.1 产品背景与定位

飞猪旅行推出独立子品牌「100种不可思议旅行」，打造**纯内容展示型平台**，专注于极致小众、视觉冲击力强、非主流的旅行方式。

- **核心价值**：为用户提供「你从未想过可以这样旅行」的独特灵感
- **产品定位**：小众旅行体验内容聚合与发现平台
- **MVP 阶段定位**：仅做内容展示，不包含任何交易、社交、用户系统功能

### 2.2 目标用户画像

| 用户类型 | 特征描述 | 核心诉求 |
|----------|----------|----------|
| Z世代探索者（95后-00后） | 数字原住民，追求个性表达，旅行决策受视觉内容驱动，乐于在社交平台分享 | 发现「能发朋友圈的」独特旅行体验 |
| 反常规生活方式追求者 | 厌倦热门景点打卡，拒绝千篇一律的旅行，追求深度独特体验 | 找到与众不同的旅行灵感，避开大众化目的地 |
| 视觉系内容消费者 | 对图片/视频质量有高要求，认为旅行的重要目的是产出高质量视觉内容 | 浏览高质量沉浸式旅行视觉素材，获取审美享受 |

### 2.3 核心用户痛点与解决方案

| 痛点 | 解决方案 |
|------|----------|
| 传统旅行App内容同质化严重，首页全是热门景点和酒店，难以发现新鲜玩法 | 专注小众非主流旅行体验，首页以视觉瀑布流直接呈现差异化内容 |
| 小众旅行体验分散在各个社交平台，没有统一的聚合入口，查找成本高 | 提供一个垂直聚合平台，一站式浏览所有小众旅行体验 |
| 现有内容多为简单打卡，缺乏故事性和情感共鸣 | 每条旅行内容包含深度叙事长文，以故事打动用户而非简单罗列信息 |
| 筛选维度单一，只能按目的地、价格等常规维度查找 | 提供「体验类型」「视觉风格」「小众程度」三个专属筛选维度，贴合目标用户决策逻辑 |

### 2.4 MVP 范围界定

**MVP 必做功能（4项）**

1. 首页瀑布流：以卡片形式展示旅行内容列表
2. 多维度筛选：提供体验类型、视觉风格、小众程度三个筛选维度
3. 关键词搜索：支持按关键词查找旅行内容
4. 详情页：沉浸式展示单个旅行体验的完整内容与素材

**MVP 明确不做功能**

注册/登录、收藏/点赞、评论/留言、分享功能、预订/购买/支付、个人中心、社交关注、足迹记录、地图视图、UGC内容发布、推送通知、CMS内容管理后台（MVP阶段内容由运营直接录入数据库或静态配置文件）

---

## 3. 产品信息架构

### 3.1 页面层级结构

```
首页（根页面）
│
├── 顶栏：品牌标识 + 搜索入口
├── 筛选组件：体验类型 / 视觉风格 / 小众程度
├── 卡片瀑布流（旅行体验列表）
│
└── 点击卡片 ──→ 详情页
                     │
                     ├── 顶部媒体轮播
                     ├── 核心信息区
                     ├── 标题与标签
                     ├── 故事叙述区
                     ├── 高清图集
                     └── 相关推荐

搜索流程：
首页顶栏搜索入口 ──→ 搜索页
                      │
                      ├── 搜索框（自动聚焦）
                      ├── 实时联想建议
                      └── 搜索结果列表（复用卡片组件）
                            │
                            └── 点击卡片 ──→ 详情页
```

### 3.2 页面间跳转逻辑

| 起点 | 终点 | 触发操作 | 返回行为 |
|------|------|----------|----------|
| 首页卡片 | 详情页 | 点击卡片 | 返回首页，保持原滚动位置和筛选状态 |
| 首页顶栏搜索图标 | 搜索页 | 点击搜索图标 | 返回首页 |
| 搜索建议/结果卡片 | 详情页 | 点击建议项或结果卡片 | 返回搜索页，保持搜索结果列表 |
| 详情页相关推荐卡片 | 另一详情页 | 点击推荐卡片 | 逐层返回（新详情页 → 原详情页 → 来源页） |

---

## 4. 首页

### 4.1 页面整体结构

首页由三个区域自上而下组成：

1. **顶栏**（固定在页面顶部，不随内容滚动消失）
2. **筛选组件**（紧贴顶栏下方，卡片流上方；用户向下滚动浏览卡片时，筛选栏吸顶固定）
3. **旅行卡片瀑布流**（可滚动内容区）

### 4.2 筛选组件

#### 4.2.1 筛选组件结构

筛选组件包含三个筛选维度，以横向排列的标签形式展示。每个维度默认收起为一行，点击展开显示全部可选项。

#### 4.2.2 筛选维度字段定义

**维度一：体验类型**

| 字段 | 说明 |
|------|------|
| 字段名 | experience_type |
| 类型 | 多选标签（Array of String） |
| 可选值 | 水下 / 高空 / 极地 / 荒野 / 地下 / 极限运动 / 文化沉浸 / 夜间奇观 / 慢旅行 |
| 默认值 | 空（不筛选，展示全部） |
| 是否必填 | 否 |
| 交互方式 | 点击标签切换选中/取消；支持多选；选中标签高亮显示 |
| 单个旅行内容 | 可关联多个体验类型 |

**维度二：视觉风格**

| 字段 | 说明 |
|------|------|
| 字段名 | visual_style |
| 类型 | 多选标签（Array of String） |
| 可选值 | 极简 / 赛博朋克 / 胶片复古 / 高饱和 / 黑白 / 电影感 / 自然光 |
| 默认值 | 空（不筛选，展示全部） |
| 是否必填 | 否 |
| 交互方式 | 点击标签切换选中/取消；支持多选；选中标签高亮显示 |
| 单个旅行内容 | 可关联多个视觉风格 |

**维度三：小众程度**

| 字段 | 说明 |
|------|------|
| 字段名 | rarity_level |
| 类型 | 单选标签（String） |
| 可选值 | 冷门（极少人知道）/ 小众（圈内知名）/ 新兴（开始流行但未大众化） |
| 默认值 | 空（不筛选，展示全部） |
| 是否必填 | 否 |
| 交互方式 | 点击标签选中；同一时间仅可选中一个；再次点击已选中标签可取消 |
| 单个旅行内容 | 有且仅有一个小众程度等级 |

#### 4.2.3 筛选交互规则

| 交互场景 | 系统行为 |
|----------|----------|
| 展开维度 | 点击维度名称，展开该维度的选项列表（下拉或内嵌展开），再次点击收起 |
| 选中选项 | 选项标签变为高亮状态，同时该维度名称旁出现已选数量角标 |
| 取消选项 | 选项标签恢复默认状态；若该维度已选数量降为0，角标消失 |
| 组合筛选 | 三个维度的选中条件取**交集**，实时更新瀑布流（无需点击「确认」按钮） |
| 一键重置 | 筛选栏最右侧显示「重置」按钮，点击后清除所有已选条件，恢复默认全部展示 |
| 筛选结果为空 | 瀑布流区域显示空状态：提示文案「没有找到匹配的旅行体验，试试调整筛选条件」，并提供「清除筛选」快捷按钮 |
| 滚动行为 | 用户向下滚动浏览卡片时，筛选栏吸顶固定，始终保持可见 |

### 4.3 旅行卡片

#### 4.3.1 卡片字段定义

| 字段名 | 类型 | 取值范围/格式 | 是否必填 | 说明 |
|--------|------|---------------|----------|------|
| cover_image | URL(String) | 图片URL，支持 WebP/JPG/PNG，最小分辨率 750×1000px | 是 | 卡片封面图，竖版构图为主 |
| title | String | 1-30个字符 | 是 | 旅行体验标题 |
| subtitle | String | 1-20个字符 | 是 | 一句话描述，吸引用户点击 |
| experience_type_tags | Array of String | 枚举值，见 4.2.2 | 是 | 体验类型标签，至少1个 |
| visual_style_tags | Array of String | 枚举值，见 4.2.2 | 是 | 视觉风格标签，至少1个 |
| rarity_level | String | 冷门 / 小众 / 新兴 | 是 | 小众程度 |
| destination | String | 1-30个字符 | 是 | 目的地名称 |
| duration | String | 自由文本，如「3天2晚」「7天」「半日」 | 是 | 建议体验时长 |

#### 4.3.2 卡片展示规则

- 卡片采用**瀑布流布局**，每行两列，卡片高度根据封面图比例自适应
- 封面图上方叠加**小众程度角标**（例如「冷门」用紫色标签）
- 封面图下方依次展示：体验类型标签（小号胶囊）→ 标题（加粗）→ 一句话描述（灰色小字）→ 目的地+时长（底部一行）
- 图片加载中显示灰色占位块（带微弱渐变动画）
- 图片加载失败显示默认占位图 + 裂图图标

### 4.4 瀑布流交互规则

| 交互场景 | 系统行为 |
|----------|----------|
| 首次加载 | 进入首页后，展示全屏骨架屏（卡片占位轮廓+闪烁动画），数据加载完成后骨架屏消失，卡片渐显 |
| 默认排序 | 按内容创建时间倒序排列（最新发布在前）。注意：若用户已完成筛选，筛选结果仍按时间倒序 |
| 滚动加载更多 | 当用户滚动到距离底部约2张卡片高度时，自动加载下一页，每页加载 20 条 |
| 加载中 | 瀑布流底部显示加载指示器（简约转圈动画 + 文字「发现更多不可思议...」） |
| 加载完毕 | 底部显示终止提示「— 已展示全部旅行体验 —」 |
| 点击卡片 | 以右滑过渡动画进入该旅行体验的详情页 |
| 筛选变更时 | 清空当前列表，从顶部重新加载（无动画，直接刷新为筛选结果） |

---

## 5. 搜索功能

### 5.1 搜索入口

首页顶栏右侧设搜索图标（放大镜），点击进入搜索页。

### 5.2 搜索页结构

| 区域 | 说明 |
|------|------|
| 搜索框 | 位于页面顶部，自动获取焦点并弹起键盘 |
| 搜索建议区 | 输入过程中实时展示联想建议列表；未输入时展示历史搜索词（若有）；无历史则展示热门搜索词 |
| 搜索结果区 | 确认搜索后展示，复用首页卡片组件布局（瀑布流） |

### 5.3 搜索字段定义

**搜索框**

| 字段 | 说明 |
|------|------|
| 字段名 | search_keyword |
| 类型 | String |
| 取值范围 | 1-50个字符 |
| 是否必填 | 是（执行搜索时） |
| 占位文案 | 「搜索旅行体验、目的地、标签...」 |

**搜索建议项**

| 字段名 | 类型 | 取值范围 | 是否必填 | 说明 |
|--------|------|----------|----------|------|
| suggestion_text | String | 1-30个字符 | 是 | 建议词文本 |
| suggestion_type | String | title / tag / destination | 是 | 建议词类型：匹配标题/匹配标签/匹配目的地 |
| target_experience_id | String | — | 否 | 若建议词直接对应唯一旅行体验，记录其ID用于直达详情页 |

**搜索结果卡片**：复用首页旅行卡片字段定义（见 4.3.1），追加高亮匹配关键词的显示。

### 5.4 搜索交互规则

| 交互场景 | 系统行为 |
|----------|----------|
| 进入搜索页 | 搜索框自动聚焦，弹出键盘；搜索框下方显示热门搜索词标签列表 |
| 输入关键词 | 输入字符后 300ms 防抖，触发联想搜索，匹配范围包括：标题、体验类型标签、视觉风格标签、目的地名称 |
| 联想建议展示 | 以列表形式展示，每项显示建议文本 + 类型标记（如「目的地」用灰色小字）；最多展示 10 条 |
| 点击联想建议 | 若该建议对应唯一旅行体验，直接跳转详情页；否则以该建议词执行搜索，展示结果列表 |
| 键盘确认搜索 | 隐藏联想建议列表，以当前输入内容执行搜索，切换至搜索结果视图 |
| 搜索结果排序 | 按关键词匹配度降序（标题完全匹配 > 标题部分匹配 > 标签匹配 > 目的地匹配），同等匹配度按时间倒序 |
| 搜索结果为空 | 展示空状态插画 + 文案「没有找到与"{关键词}"相关的旅行体验」，下方展示热门搜索词供用户探索 |
| 搜索结果与筛选联动 | 若用户在首页已设置筛选条件再进入搜索，搜索范围限定在筛选结果内；搜索页顶部显示当前筛选条件标签 |
| 清除搜索 | 搜索框右侧显示清除按钮（×），点击清空输入框内容，恢复搜索建议视图 |
| 返回首页 | 点击左上角返回箭头或手势右滑，返回首页，保持首页原筛选状态和滚动位置 |

---

## 6. 详情页

### 6.1 页面结构

详情页按自上而下的顺序排列以下模块：

| 顺序 | 模块 | 设计意图 |
|------|------|----------|
| 1 | 顶部媒体轮播区 | 视觉冲击，建立第一印象 |
| 2 | 核心信息区 | **实用信息优先**，让用户快速判断「我能不能去、什么时候去、难不难」 |
| 3 | 标题与标签区 | 确认体验身份和分类 |
| 4 | 故事叙述区 | 用故事激发情感共鸣和出行欲望 |
| 5 | 高清图集区 | 提供更多视觉素材，满足浏览和保存需求 |
| 6 | 相关推荐 | 提供备选方案，延长用户停留和探索 |

### 6.2 各模块字段定义

#### 6.2.1 顶部媒体轮播区

| 字段名 | 类型 | 取值范围/格式 | 是否必填 | 说明 |
|--------|------|---------------|----------|------|
| media_list | Array of Object | 每个元素包含 type、url、alt | 是 | 媒体资源列表，至少1项 |
| media_list[].type | String | image / video | 是 | 资源类型 |
| media_list[].url | URL(String) | 资源地址，图片最小宽度 1080px，视频支持 MP4 | 是 | 资源URL |
| media_list[].alt | String | 1-100个字符 | 是 | 图片无障碍替代文本 |
| media_list[].cover_frame | URL(String) | — | 否 | 视频封面帧（type为video时必填） |
| auto_play | Boolean | true / false | 是 | 是否自动轮播，默认 true，间隔 3 秒 |

#### 6.2.2 核心信息区（实用信息优先）

| 字段名 | 类型 | 取值范围/格式 | 是否必填 | 说明 |
|--------|------|---------------|----------|------|
| destination | String | 1-30个字符 | 是 | 目的地名称 |
| destination_region | String | 1-30个字符 | 否 | 所属地区/国家 |
| duration | String | 自由文本，如「3天2晚」「7天」「半日」 | 是 | 建议体验时长 |
| best_season | String | 自由文本，如「6月-9月」「全年」「12月-次年2月」 | 是 | 最佳体验季节 |
| difficulty_level | String | 入门 / 中等 / 硬核 / 极限 | 是 | 难度等级 |

> **设计说明**：核心信息区紧接媒体轮播之后、故事叙事之前。用户浏览完媒体素材后，第一个理性问题是「我能不能去？」，将目的地、时长、季节、难度四个关键决策字段前置，减少滚动查找摩擦。

#### 6.2.3 标题与标签区

| 字段名 | 类型 | 取值范围/格式 | 是否必填 | 说明 |
|--------|------|---------------|----------|------|
| title | String | 1-30个字符 | 是 | 旅行体验标题 |
| experience_type_tags | Array of String | 枚举值，见 4.2.2 | 是 | 体验类型标签 |
| visual_style_tags | Array of String | 枚举值，见 4.2.2 | 是 | 视觉风格标签 |
| rarity_level | String | 冷门 / 小众 / 新兴 | 是 | 小众程度 |

#### 6.2.4 故事叙述区

| 字段名 | 类型 | 取值范围/格式 | 是否必填 | 说明 |
|--------|------|---------------|----------|------|
| story_title | String | 1-50个字符 | 是 | 故事标题 |
| story_body | String | 富文本，支持标题、段落、加粗、引用块 | 是 | 故事正文，建议 500-3000 字 |
| story_highlights | Array of String | 每项1-50个字符 | 否 | 故事亮点摘要列表（3-5条），以项目符号展示在正文前 |

#### 6.2.5 高清图集区

| 字段名 | 类型 | 取值范围/格式 | 是否必填 | 说明 |
|--------|------|---------------|----------|------|
| gallery_images | Array of Object | 至少2项 | 是 | 图集列表 |
| gallery_images[].url | URL(String) | 图片最小宽度 1080px | 是 | 高清图片URL |
| gallery_images[].alt | String | 1-100个字符 | 是 | 图片描述 |
| gallery_images[].caption | String | 1-100个字符 | 否 | 图片说明文字，在图片下方展示 |

#### 6.2.6 相关推荐区

| 字段名 | 类型 | 取值范围/格式 | 是否必填 | 说明 |
|--------|------|---------------|----------|------|
| related_experiences | Array of Object | 2-4项 | 是 | 推荐内容列表，每项复用首页卡片字段（见 4.3.1） |
| recommendation_rule | String | same_type / same_style / same_region | 是 | 推荐逻辑标签，在推荐区上方展示如「同类型体验」「同风格体验」 |

### 6.3 详情页交互规则

| 交互场景 | 系统行为 |
|----------|----------|
| 媒体轮播 | 手指左右滑动切换媒体项；图片支持双指缩放；视频默认静音自动播放，点击后展开全屏并开启声音 |
| 媒体全屏模式 | 点击媒体区域进入全屏浏览，隐藏其他页面元素；全屏模式下左右滑动切换，上滑或点击关闭按钮退出全屏 |
| 图片渐进加载 | 图片先加载低质量模糊版本（占位），再替换为高清版本，过渡时间约 300ms |
| 图集大图浏览 | 点击图集中任意图片进入大图模式，支持左右滑动切换、双指缩放；顶部显示当前序号（如 3/12）；点击空白区域或关闭按钮退出 |
| 相关推荐点击 | 点击推荐卡片以右滑动画进入新的详情页；返回时逐层返回 |
| 返回上一页 | 点击左上角返回按钮或屏幕边缘右滑手势，返回上一页（首页或搜索页），保持原页面状态 |
| 页面滚动 | 自由滚动浏览全部模块；顶栏随滚动隐藏，向上滚动时重新出现 |
| 内容失效处理 | 若旅行内容已下架或数据异常，展示提示「该旅行内容暂不可用」，提供「返回首页」按钮 |

---

## 7. 全局交互规范

### 7.1 页面过渡动画

| 过渡场景 | 动画效果 |
|----------|----------|
| 首页 → 详情页 | 卡片放大+右滑进入，持续约 300ms |
| 详情页 → 首页 | 左滑退出，持续约 300ms |
| 首页 → 搜索页 | 搜索页从底部上滑弹出（模态），持续约 250ms |
| 搜索页 → 首页 | 向下滑出（或点击返回），持续约 250ms |
| 详情页 → 详情页（相关推荐跳转） | 右滑进入新详情页，持续约 300ms |

### 7.2 加载状态

| 场景 | 加载样式 | 说明 |
|------|----------|------|
| 首页首次加载 | 全屏骨架屏 | 模拟卡片瀑布流布局轮廓，带微微闪烁动画 |
| 瀑布流加载更多 | 底部加载指示器 | 简约转圈动画 + 文案「发现更多不可思议...」 |
| 图片加载（卡片/详情） | 渐进式加载 | 低质量占位（模糊+单色底）→ 高清图淡入，过渡约 300ms |
| 详情页首次打开 | 媒体区骨架屏 + 下方内容骨架屏 | 图片轮播区和文字区分别占位 |
| 搜索联想 | 联想列表区短暂 loading 动画 | 输入停止后 300ms 内展示小菊花 |

### 7.3 空状态

| 场景 | 展示内容 |
|------|----------|
| 筛选无结果 | 插画图标 + 文案「没有找到匹配的旅行体验」+ 副文案「试试调整筛选条件」+ 按钮「清除筛选」 |
| 搜索无结果 | 插画图标 + 文案「没有找到与"{关键词}"相关的旅行体验」+ 副文案「试试其他关键词」+ 热门搜索词标签 |
| 详情页内容失效 | 插画图标 + 文案「该旅行内容暂不可用」+ 按钮「返回首页」 |

### 7.4 异常状态

| 场景 | 展示内容 |
|------|----------|
| 网络错误（内容加载失败） | 插画图标 + 文案「网络连接失败」+ 按钮「点击重试」 |
| 加载超时（超过 10 秒） | 插画图标 + 文案「加载超时」+ 副文案「请检查网络后重试」+ 按钮「重新加载」 |
| 图片加载失败（单张） | 灰色占位背景 + 裂图图标居中显示；不影响其他图片和文字展示 |

### 7.5 导航规则

| 规则 | 说明 |
|------|------|
| 首页为根页面 | 用户在任何页面连续返回，最终回到首页 |
| 返回保持状态 | 从详情页返回首页时，保持首页的滚动位置和筛选状态 |
| 搜索页为模态 | 搜索页以模态形式呈现，不从属于页面栈，关闭后直接回到发起搜索的页面 |
| 无底部TabBar | MVP 阶段仅首页一个主页面，不设底栏导航 |

---

## 8. 非功能需求

### 8.1 性能要求

| 指标 | 要求 | 说明 |
|------|------|------|
| 首页首屏加载时间 | ≤ 2 秒（4G网络环境） | 从点击图标到首屏卡片展示的时间 |
| 图片懒加载 | 仅加载可视区域+预加载一屏内容 | 减少初始加载流量和时间 |
| 详情页首屏加载 | ≤ 1.5 秒 | 首张封面图 + 核心信息区可见即算首屏 |
| 搜索联想响应 | ≤ 300ms | 输入停止后到联想结果展示的时间 |
| 瀑布流加载更多 | 新卡片展示 ≤ 1 秒 | 滚动触发到新卡片可见的时间 |

### 8.2 兼容性

| 平台 | 要求 |
|------|------|
| iOS | iOS 14 及以上，覆盖 Safari / 微信内置浏览器 |
| Android | Android 10 及以上，覆盖 Chrome / 主流厂商系统浏览器 / 微信内置浏览器 |
| 屏幕适配 | 适配 375px-428px 宽度的主流手机屏幕，含异形屏（刘海/挖孔/灵动岛）安全区适配 |
| 方向 | 仅支持竖屏（Portrait），锁定横屏旋转 |

### 8.3 内容质量要求

| 类型 | 要求 |
|------|------|
| 封面图 | 竖版构图，最小分辨率 750×1000px，宽高比建议 3:4 |
| 详情页媒体 | 图片最小宽度 1080px；视频分辨率 ≥ 720p |
| 图片格式 | 优先使用 WebP 格式，兼容 JPG/PNG 作为降级 |
| 无障碍 | 所有图片必须填写 alt 文本 |
| 文字对比度 | 正文文字与背景色对比度满足 WCAG AA 标准（至少 4.5:1） |

### 8.4 可访问性

| 要求 | 说明 |
|------|------|
| 图片 alt 文本 | 所有内容图片必须提供准确的描述性 alt 文本 |
| 色彩对比度 | 文字与背景色对比度 ≥ 4.5:1（WCAG AA） |
| 触摸目标 | 所有可交互元素的最小触摸区域 ≥ 44×44pt（iOS）/ 48×48dp（Android） |
| 字体缩放 | 支持系统字体缩放至 130%，页面布局不破版 |

---

## 9. 上线标准

### 9.1 功能完整性检查清单

| 序号 | 检查项 | 通过标准 |
|------|--------|----------|
| 1 | 首页瀑布流展示 | 正常展示 ≥ 20 条旅行内容；卡片信息完整（封面、标题、描述、标签、目的地、时长） |
| 2 | 瀑布流分页加载 | 滚动到底部自动加载下一页，加载完毕后显示终止提示 |
| 3 | 体验类型筛选 | 每个选项可独立选中/取消；多选结果正确展示 |
| 4 | 视觉风格筛选 | 每个选项可独立选中/取消；多选结果正确展示 |
| 5 | 小众程度筛选 | 单选逻辑正确；点击已选标签可取消 |
| 6 | 组合筛选 | 三维度交叉筛选结果正确（取交集） |
| 7 | 一键重置 | 点击重置后所有筛选条件清除，恢复全部内容展示 |
| 8 | 筛选与搜索联动 | 先筛选再搜索，搜索结果限定在筛选结果范围内 |
| 9 | 搜索联想 | 输入关键词后正确匹配标题、标签、目的地，展示 ≤ 10 条建议 |
| 10 | 搜索结果展示 | 搜索结果列表正确展示，支持点击进入详情页 |
| 11 | 搜索空状态 | 无匹配结果时正确展示空状态和热门搜索词 |
| 12 | 详情页媒体轮播 | 图片/视频正确加载，支持左右滑动切换 |
| 13 | 详情页核心信息 | 目的地、时长、季节、难度四个字段完整显示 |
| 14 | 详情页故事 | 富文本正确渲染 |
| 15 | 详情页图集 | 网格式展示，点击可查看大图并切换 |
| 16 | 详情页相关推荐 | 展示 2-4 个推荐卡片，点击可跳转 |
| 17 | 空状态 | 筛选无结果、搜索无结果、内容失效三个场景正确展示 |
| 18 | 异常状态 | 网络错误、加载超时场景可重试并恢复 |

### 9.2 质量门禁

| 序号 | 门禁项 | 通过标准 |
|------|--------|----------|
| 1 | 字段完整性 | 所有页面/模块字段定义无遗漏，与 PRD 一致 |
| 2 | 状态覆盖 | 所有空状态、异常状态、加载状态均覆盖 |
| 3 | 图片兜底 | 所有图片加载失败时有占位图，不影响页面布局 |
| 4 | 机型适配 | 5 款主流机型（iPhone 14/15/SE、小米14、华为Pura70）视觉走查通过 |
| 5 | 性能达标 | 首屏加载时间 ≤ 2 秒（4G），详情页 ≤ 1.5 秒 |
| 6 | 无Crash | 核心流程压测 1 小时无闪退 |

### 9.3 内容就绪标准

| 序号 | 标准 | 说明 |
|------|------|------|
| 1 | 内容数量 | 上线前至少录入 **50 条**旅行体验完整内容 |
| 2 | 内容覆盖 | 每个体验类型至少覆盖 3 条内容；每个视觉风格至少覆盖 5 条内容；三个小众程度级别均有覆盖 |
| 3 | 内容完整性 | 每条内容包含：标题、描述、封面图、至少 1 个体验类型标签、至少 1 个视觉风格标签、小众程度等级、目的地、时长、最佳季节、难度等级、故事正文、至少 5 张图集图片 |
| 4 | 图片质量 | 所有图片分辨率达标（封面 ≥ 750×1000px，详情图 ≥ 1080px宽） |

---

## 10. 数据字典汇总

### 10.1 旅行体验（Experience）

| 字段名 | 类型 | 取值范围 | 必填 | 所属页面 |
|--------|------|----------|------|----------|
| id | String | 全局唯一标识 | 是 | 全局 |
| title | String | 1-30字符 | 是 | 首页卡片、详情页 |
| subtitle | String | 1-20字符 | 是 | 首页卡片 |
| cover_image | URL | 最小 750×1000px | 是 | 首页卡片 |
| experience_type_tags | Array\<String\> | 水下/高空/极地/荒野/地下/极限运动/文化沉浸/夜间奇观/慢旅行 | 是 | 首页卡片、详情页、筛选 |
| visual_style_tags | Array\<String\> | 极简/赛博朋克/胶片复古/高饱和/黑白/电影感/自然光 | 是 | 首页卡片、详情页、筛选 |
| rarity_level | String | 冷门/小众/新兴 | 是 | 首页卡片、详情页、筛选 |
| destination | String | 1-30字符 | 是 | 首页卡片、详情页 |
| destination_region | String | 1-30字符 | 否 | 详情页 |
| duration | String | 自由文本 | 是 | 首页卡片、详情页 |
| best_season | String | 自由文本 | 是 | 详情页 |
| difficulty_level | String | 入门/中等/硬核/极限 | 是 | 详情页 |
| media_list | Array\<Object\> | type+url+alt | 是 | 详情页 |
| story_title | String | 1-50字符 | 是 | 详情页 |
| story_body | String | 富文本 | 是 | 详情页 |
| story_highlights | Array\<String\> | 3-5项 | 否 | 详情页 |
| gallery_images | Array\<Object\> | url+alt+caption | 是 | 详情页 |
| related_experiences | Array\<Object\> | 2-4项 | 是 | 详情页 |
| recommendation_rule | String | same_type/same_style/same_region | 是 | 详情页 |
| created_at | DateTime | ISO 8601 | 是 | 排序依据 |

---

## 11. 附录：MVP 明确不做功能清单

以下功能**不在 MVP 范围内**，禁止在设计和开发阶段引入：

| 序号 | 不做功能 | 备注 |
|------|----------|------|
| 1 | 用户注册/登录 | 无用户系统 |
| 2 | 收藏/喜欢 | 无用户系统支撑 |
| 3 | 评论/留言 | 无社交互动功能 |
| 4 | 分享功能（含微信/微博/复制链接） | 用户可通过系统截图自行分享 |
| 5 | 预订/购买/支付 | 无交易功能，纯内容展示 |
| 6 | 个人中心 | 无用户系统 |
| 7 | 社交关注/粉丝 | 无社交关系链 |
| 8 | 足迹/浏览历史记录 | 无用户数据持久化 |
| 9 | 地图视图 | 以文本目的地信息替代 |
| 10 | UGC 内容发布 | 所有内容由运营录入 |
| 11 | 推送通知 | 无消息系统 |
| 12 | 内容 CMS 管理后台 | MVP 阶段内容直接由运营配置数据文件录入，不做后台 |
| 13 | 底部 TabBar 导航 | 仅首页一个主页面 |
| 14 | 横屏模式 | 仅竖屏 |

---
*文档结束*
