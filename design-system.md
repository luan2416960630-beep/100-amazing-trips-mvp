# 飞猪「100种不可思议旅行」UI 设计风格规范

> **文档版本**：v1.0 | **创建日期**：2026-06-04 | **基于 PRD**：v1.0  
> **面向人群**：UI 设计师、前端开发工程师  
> **设计定位**：Z 世代极客探索者 + 视觉系内容消费者的专属旅行灵感平台

---

## 1. 整体设计原则

### 1.1 风格定位

| 关键词 | 设计语言诠释 |
|--------|-------------|
| **极简暗黑** | 深黑色全局背景 (#0a0a0a)，移除所有不必要的装饰元素，让页面只剩下内容和必要操作。暗色背景天然降低屏幕亮度，用户注意力被引导至高亮图片区域。 |
| **高对比度** | 纯白文字 (#ffffff) 与深黑背景 (#0a0a0a) 形成 21:1 对比度，远超 WCAG AAA 标准的 7:1。荧光蓝 (#00f0ff) 作为唯一强调色在暗色背景上高度醒目。 |
| **沉浸式** | 全屏媒体轮播、无边栏单列瀑布流、无底栏导航。用户从进入首页开始就被图片包裹，如同翻阅一本精心编排的摄影集。 |
| **未来感** | 荧光蓝品牌色取自赛博朋克视觉体系中的霓虹色调。毛玻璃导航栏、圆角卡片悬浮效果、无阴影扁平化处理共同构建「近未来」数字质感。 |

### 1.2 核心设计原则

**图片优先（Image-First Hierarchy）**

```
视觉优先级：图片 > 标题 > 标签 > 辅助文字
注意力分配：80% 图片区域 / 20% 文字信息
```

- 任何页面中，首屏的 70% 以上面积由高质量视觉内容占据
- 文字永远位于图片下方或叠加于图片底部渐变遮罩上
- 文字不侵入图片的核心视觉区域（图片上半部分 60% 面积）
- 所有 UI 控件（按钮、标签、指示器）使用半透明材质，不遮挡图片内容

### 1.3 情绪基调

| 正面情绪目标 | 具体感受 | 设计手段 |
|-------------|---------|----------|
| **神秘** | 「这是一个未知的世界在等我」 | 暗色背景中仅图片高亮，如同黑暗房间中的一扇窗 |
| **好奇** | 「下一个会是什么？」 | 瀑布流无页码、无进度条，无限下拉激发探索欲 |
| **高级** | 「这不是大众旅游产品」 | 克制配色（仅 1 个品牌色 + 3 级灰度）、大留白、无广告感 |
| **小众** | 「这是只有少数人知道的」 | 冷门/小众/新兴标签以荧光蓝高亮，传递稀缺性信号 |

**严格避免**：暖色调大面积使用、弹窗促销、倒计时、价格划线、勋章图标、星星评分——任何带有「催促消费」视觉暗示的元素。

---

## 2. 颜色系统

### 2.1 基础色

| Token 名称 | 十六进制 | RGB | 用途 | CSS 变量 |
|-----------|----------|-----|------|----------|
| `bg-primary` | `#0a0a0a` | `10, 10, 10` | 所有页面默认背景 | `--color-bg-primary` |
| `text-primary` | `#ffffff` | `255, 255, 255` | 一级文字（标题、正文） | `--color-text-primary` |
| `text-secondary` | `#a0a0a0` | `160, 160, 160` | 二级文字（说明、辅助信息） | `--color-text-secondary` |
| `text-tertiary` | `#666666` | `102, 102, 102` | 禁用状态、占位符文字 | `--color-text-tertiary` |

**对比度验证**：

| 前景色 | 背景色 | 对比度 | 达标等级 |
|--------|--------|--------|----------|
| `#ffffff` | `#0a0a0a` | 21.0:1 | AAA (≥7:1) |
| `#a0a0a0` | `#0a0a0a` | 7.9:1 | AAA (≥4.5:1) |
| `#666666` | `#0a0a0a` | 3.4:1 | 仅用于装饰性/禁用元素 |
| `#00f0ff` | `#0a0a0a` | 15.2:1 | AAA (≥7:1) |

### 2.2 品牌色

| Token 名称 | 值 | 用途 | CSS 变量 |
|-----------|-----|------|----------|
| `brand-primary` | `#00f0ff` | 按钮背景、链接文字、选中状态、强调元素 | `--color-brand` |
| `brand-gradient` | `#00f0ff` → `#0080ff` | 按钮悬停态、进度条、加载动画 | `--color-brand-gradient` |
| `brand-glow` | `rgba(0, 240, 255, 0.10)` | 卡片悬停光晕、选中标签背景 | `--color-brand-glow` |
| `brand-glow-strong` | `rgba(0, 240, 255, 0.20)` | 按钮悬停光晕、焦点环 | `--color-brand-glow-strong` |

**品牌色使用规则**：
- 每个视口内品牌色出现不超过 3 处（保持稀缺感和高级感）
- 品牌色不作为大面积背景色使用（仅用于点状强调）
- 品牌色文字仅用于可交互元素（链接、按钮），不用于纯展示文字

### 2.3 功能色

| Token 名称 | 十六进制 | RGB | 用途 | CSS 变量 |
|-----------|----------|-----|------|----------|
| `error` | `#ff4444` | `255, 68, 68` | 错误状态、删除操作 | `--color-error` |
| `success` | `#00ff88` | `0, 255, 136` | 成功状态（极简使用） | `--color-success` |
| `warning` | `#ffaa00` | `255, 170, 0` | 警告提示 | `--color-warning` |

**功能色使用规则**：
- 错误色 `#ff4444` 仅用于网络错误、加载失败的重试按钮（与 PRD 7.4 异常状态对齐）
- 成功色 `#00ff88` MVP 阶段极少使用（无用户操作反馈）
- 功能色不用于旅行内容标签或分类

### 2.4 卡片文字渐变遮罩

```
/* 卡片底部文字保护层 */
background: linear-gradient(
    to bottom,
    transparent 0%,
    rgba(0, 0, 0, 0) 40%,
    rgba(0, 0, 0, 0.6) 70%,
    rgba(0, 0, 0, 0.85) 100%
);
高度: 120px;
位置: 卡片底部;
```

---

## 3. 排版系统

### 3.1 字体

| 属性 | 值 |
|------|-----|
| 主字体 | **Inter**（Google Fonts / CDN 引入） |
| 备用字体 | `-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` |
| 等宽字体（代码/数据） | `"JetBrains Mono", "SF Mono", monospace` |
| 字体引入 | `@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');` |

**选择理由**：Inter 是为屏幕阅读优化的无衬线字体，x-height 较高（提升小字号可读性），字形中性克制不干扰图片内容，支持完整的 CJK fallback。

### 3.2 文字层级

| 层级 | Token | 字号 | 字重 | 行高 | 字间距 | 使用场景 |
|------|-------|------|------|------|--------|----------|
| **H1** | `text-h1` | 48px / 64px\(^1\) | 700 (Bold) | 1.1 | `-0.02em` | 页面主标题、卡片大标题 |
| **H2** | `text-h2` | 24px / 32px\(^1\) | 600 (SemiBold) | 1.2 | `-0.01em` | 详情页标题、模块标题 |
| **Body** | `text-body` | 16px | 400 (Regular) | 1.5 | `0` | 正文、故事内容、详细介绍 |
| **Small** | `text-small` | 14px | 400 (Regular) | 1.4 | `0` | 标签文字、时长/季节信息 |
| **Caption** | `text-caption` | 12px | 500 (Medium) | 1.3 | `0.005em` | 版权、提示、图集序号 |

> \(^1\) 手机端 / 桌面端。平板端使用 48px。

### 3.3 排版规则

| 规则 | 参数 | 说明 |
|------|------|------|
| 对齐方式 | **左对齐（默认）** | 所有标题、正文、标签一律左对齐。居中仅用于空状态、加载状态和错误状态 |
| 标题截断 | **最多 2 行** | `line-clamp: 2`，超出显示省略号 `…`。H1 第一行优先承载核心信息 |
| 段落最大宽度 | **600px** | 详情页正文 `max-width: 600px`，保证最佳阅读行长（约 75 个中文字符/行） |
| 段落间距 | **1.5 倍行高** | 相邻 `<p>` 之间 `margin-bottom: 1.5em`（即 24px，body 行高 16px × 1.5） |
| 列表项间距 | **8px** | `<li>` 之间 `margin-bottom: 8px` |
| 英文/数字 | **Tabular Nums** | 所有数字使用 `font-variant-numeric: tabular-nums`，保证对齐 |

---

## 4. 间距系统

采用 **8px 基础网格系统**，所有间距值必须为 8 的整数倍。

| Token | 值 | 用途 |
|-------|-----|------|
| `space-xs` | 8px | 最小间距：标签之间、图标与文字之间 |
| `space-sm` | 16px | 基础间距：卡片内边距、文字与图片之间 |
| `space-md` | 24px | 页面边距（手机）、卡片间距（手机） |
| `space-lg` | 32px | 模块间距：筛选栏与卡片之间、详情页模块之间 |
| `space-xl` | 48px | 页面边距（平板） |
| `space-2xl` | 64px | 页面底部留白 |
| `space-3xl` | 80px | 页面边距（桌面）、详情页顶部留白 |

### 留白策略

```
┌─────────────────────────────┐
│  ← 24px 页面边距 →          │  ← 手机端
│  [        顶栏        ]     │
│  ← 24px →              ← 24px
│  [      筛选组件       ]     │
│  ← 16px ↓                    │  ← 筛选栏与卡片流间距 16px
│  ┌─────────────────────┐    │
│  │                     │    │
│  │     封面图片        │    │
│  │                     │    │
│  │  ← 16px 内边距 →   │    │  ← 文字区域左右各 16px
│  │  标题 (H2)          │    │
│  │  描述 (Small)       │    │
│  │  标签 (Caption)     │    │
│  └─────────────────────┘    │
│  ← 24px 卡片间距 →          │  ← 卡片之间垂直间距
│  ┌─────────────────────┐    │
│  │    ...下一张卡片     │    │
│  └─────────────────────┘    │
│                             │
│  ← 64px 底部留白 →          │  ← 避免最后一张卡片贴底
└─────────────────────────────┘
```

### 图片与文字间距铁律

- 卡片内：封面图底部到标题顶部 **16px**
- 详情页：媒体轮播底部到核心信息区 **32px**
- 详情页：段落与段落之间 **24px**（1.5 × 16px 行高）
- 详情页：图集网格单元之间 **8px**

**禁止**：文字直接压在图片上（渐变遮罩除外，且遮罩区域内文字距离图片边缘至少 16px）。

---

## 5. 核心组件设计规范

### 5.1 旅行卡片（Card）

首页瀑布流核心组件，承担「第一印象」功能。

| 属性 | 手机端 | 平板/桌面端 |
|------|--------|------------|
| 宽高比 | 宽度占满容器，高度自适应（基于封面图原始比例） | 同左 |
| 圆角 | `border-radius: 12px` | `border-radius: 12px` |
| 边框 | `1px solid rgba(255, 255, 255, 0.08)` | `1px solid rgba(255, 255, 255, 0.08)` |
| 阴影 | 无（`box-shadow: none`） | 无 |
| 背景 | `#141414`（图片加载前的占位色） | 同左 |

**卡片内部结构（自上而下）**：

```
┌─────────────────────────────┐
│                             │
│    [封面图片区域]           │  ← 占卡片高度 70%
│    不允许任何文字侵入       │
│                             │
├─────────────────────────────┤  ← 渐变遮罩起始线（卡片 60% 高度处）
│  [小众程度角标]             │  ← 左上角绝对定位，z-index: 2
│  ← 16px →                  │
│  标题 (24px, SemiBold, 白色)│  ← line-clamp: 2
│  ← 8px ↓                   │
│  一句话描述 (14px, #a0a0a0) │  ← line-clamp: 1
│  ← 12px ↓                  │
│  [体验类型胶囊] [视觉风格]  │  ← 12px 荧光蓝文字
│  ← 8px ↓                   │
│  目的地 · 时长 (12px, #666) │
│  ← 16px ↓                  │  ← 卡片底部内边距
└─────────────────────────────┘
```

**小众程度角标规范**：

| 等级 | 背景色 | 文字色 | 文案 |
|------|--------|--------|------|
| 冷门 | `rgba(0, 240, 255, 0.15)` | `#00f0ff` | 冷门 |
| 小众 | `rgba(0, 240, 255, 0.10)` | `#00ccdd` | 小众 |
| 新兴 | `rgba(255, 170, 0, 0.12)` | `#ffaa00` | 新兴 |

角标样式：`padding: 4px 10px; border-radius: 4px; font-size: 11px; font-weight: 600; top: 12px; left: 12px;`

### 5.2 筛选栏（Filter Bar）

| 属性 | 值 |
|------|-----|
| 背景 | `rgba(10, 10, 10, 0.92)` |
| 毛玻璃 | `backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);` |
| 吸顶定位 | `position: sticky; top: 56px; z-index: 10;` |
| 高度 | 收起态 48px / 展开态 自适应 |
| 内边距 | `padding: 8px 24px`（手机）/ `padding: 8px 48px`（桌面） |
| 底部边框 | `1px solid rgba(255, 255, 255, 0.06)` |

**筛选按钮状态**：

| 状态 | 背景 | 边框 | 文字色 |
|------|------|------|--------|
| 未选中 | `transparent` | `1px solid rgba(255,255,255,0.2)` | `#ffffff` |
| 悬停（桌面） | `rgba(255,255,255,0.05)` | `1px solid rgba(255,255,255,0.4)` | `#ffffff` |
| 选中 | `#00f0ff` | `1px solid #00f0ff` | `#0a0a0a` |
| 禁用 | `transparent` | `1px solid rgba(255,255,255,0.06)` | `#666666` |

按钮样式：`height: 36px; padding: 0 16px; border-radius: 20px; font-size: 13px; font-weight: 500;`  
按钮间距：`gap: 12px;`  
展开/收起动画：`transition: all 300ms ease-out-cubic;`

**维度名称标签**（横向排列在按钮组左侧）：
- 字号 12px，字重 500，颜色 `#666666`
- 后面紧跟冒号和该维度的按钮组
- 已选数量角标：荧光蓝圆点 `width: 8px; height: 8px; border-radius: 50%;` 显示在维度名右上角

**重置按钮**（筛选栏最右侧）：
- 文字 `重置`，字号 13px，颜色 `#a0a0a0`
- 仅在有筛选条件时显示（无筛选时 `opacity: 0; pointer-events: none;`）
- 点击后所有选中按钮恢复未选态，300ms 过渡

### 5.3 详情页图片轮播（Media Carousel）

| 属性 | 值 |
|------|-----|
| 宽度 | 100vw（全屏宽） |
| 高度 | 70vh（占屏幕 70%） |
| 最小高度 | 480px（手机）/ 640px（桌面） |
| 最大高度 | 800px |
| 指示器 | **无**（极简设计，左右滑动即暗示可切换） |
| 控制按钮 | **无**（全屏模式下保留关闭按钮，见下方） |

**轮播交互**：
- 手指左右滑动切换媒体项，切换动画 `300ms ease-out-cubic`
- 当前图片支持双指缩放（pinch-zoom），最大 3 倍
- 视频项：默认静音自动循环播放（`muted autoplay loop playsinline`），点击展开全屏并开启声音
- 全屏模式：从当前位置无缝过渡放大，背景变为纯黑 `#000000`，隐藏所有页面元素，展示关闭按钮（右上角 16px，白色半透明圆形按钮，直径 40px，内含 × 图标）

**图片加载策略**：
```
占位（#141414 纯色块）→ 低分辨率预览（10-20KB LQIP）→ 高斯模糊 → 高清原图
总时长：500ms
CSS: transition: filter 400ms ease-out, opacity 400ms ease-out;
LQIP blur: filter: blur(20px);
高清: filter: blur(0);
```

**轮播底部安全区**（iPhone 刘海/灵动岛适配）：
- 图片区域底部自动避开 `safe-area-inset-bottom`
- 全屏模式下关闭按钮距离顶部 `max(16px, env(safe-area-inset-top))`

### 5.4 按钮（Button）

| 变体 | 背景 | 边框 | 文字色 | 圆角 | 高度 | 内边距 |
|------|------|------|--------|------|------|--------|
| **主按钮** | `#00f0ff` | 无 | `#0a0a0a` | 8px | 48px | `0 32px` |
| **次要按钮** | `transparent` | `1px solid #ffffff` | `#ffffff` | 8px | 48px | `0 32px` |
| **文字按钮** | `transparent` | 无 | `#00f0ff` | — | 自适应 | `0 4px` |
| **图标按钮** | `rgba(255,255,255,0.06)` | 无 | `#ffffff` | 50% | 40px | N/A |

**状态变化（主按钮）**：

| 状态 | 效果 | 参数 |
|------|------|------|
| 默认 | `background: #00f0ff` | — |
| 悬停 | 背景色加深 10% + 微弱放大 + 光晕 | `background: #00d8e5; transform: scale(1.02); box-shadow: 0 0 20px rgba(0,240,255,0.3);` |
| 按下 | 缩小 | `transform: scale(0.98); transition: 100ms;` |
| 禁用 | 整体透明度降低 | `opacity: 0.3; pointer-events: none;` |
| 加载中 | 文字替换为 20px 旋转圆环 | 圆环颜色 `#0a0a0a`（与主按钮背景对比） |

**按钮最小触摸区域**：44×44pt（iOS）/ 48×48dp（Android），小于此尺寸的按钮外围增加透明点击区域。

---

## 6. 交互与动画系统

### 6.1 动画参数统一

| 参数 | 值 | CSS 写法 |
|------|-----|----------|
| 基础过渡时长 | 300ms | `transition: all 300ms` |
| 缓动函数 | ease-out-cubic | `cubic-bezier(0.33, 1, 0.68, 1)` |
| 图片加载时长 | 500ms | 见 5.3 |
| 页面切换时长 | 300ms | 淡入淡出 |
| 展开/收起时长 | 300ms | 筛选栏、搜索建议 |
| 懒加载入场时长 | 400ms | 卡片从下向上滑入 |

### 6.2 悬停效果（桌面端）

| 元素 | 效果 | CSS |
|------|------|-----|
| **卡片** | 向上浮起 8px + 边框变为荧光蓝 + 图片放大 1.05 倍 + 荧光蓝外发光 | `transform: translateY(-8px); border-color: #00f0ff; box-shadow: 0 0 30px rgba(0,240,255,0.15);` 内部图片 `transform: scale(1.05); transition: 400ms;` |
| **主按钮** | 背景加深 10% + 放大 1.02 + 光晕 | 见 5.4 |
| **文字链接** | 颜色变为荧光蓝 + 下方出现 1px 下划线 | `color: #00f0ff; text-decoration: underline; text-underline-offset: 4px;` |
| **筛选按钮（未选中）** | 边框变亮 + 背景微亮 | `border-color: rgba(255,255,255,0.4); background: rgba(255,255,255,0.05);` |

**重要**：所有悬停效果仅在 `@media (hover: hover)` 下生效，触屏设备不触发悬停态。

### 6.3 滚动效果

| 效果 | 触发条件 | 参数 |
|------|----------|------|
| **导航栏背景变化** | 页面滚动 > 0 | 背景从 `transparent` 渐变到 `rgba(10,10,10,0.92)` + `backdrop-filter: blur(20px)`，过渡 200ms |
| **图片懒加载入场** | 图片进入视口 200px 以内 | 从 `translateY(24px); opacity: 0;` 滑入到 `translateY(0); opacity: 1;`，`transition: 400ms ease-out-cubic` |
| **新卡片追加** | 加载更多数据后 | 新卡片以 `stagger` 方式依次入场，每张延迟 60ms，从 `opacity: 0; translateY(20px)` 到完全显示 |
| **筛选栏吸顶** | 向下滚动超过顶栏 | 筛选栏 `position: sticky; top: 0;` 附带 `backdrop-filter: blur(20px);` |

### 6.4 页面过渡

| 场景 | 动画 | 时长 | 缓动 |
|------|------|------|------|
| 首页 → 详情页 | 卡片原地放大 + 右滑推入（共享元素过渡） | 300ms | `ease-out-cubic` |
| 详情页 → 首页 | 左滑退出 + 卡片缩回原位 | 300ms | `ease-in-cubic` |
| 首页 → 搜索页 | 搜索页从底部滑入（模态） | 250ms | `ease-out-cubic` |
| 搜索页 → 首页 | 向下滑出消失 | 250ms | `ease-in-cubic` |
| 详情页 → 详情页 | 右滑推入新详情页（叠加在导航栈上） | 300ms | `ease-out-cubic` |

### 6.5 微交互

| 场景 | 反馈 |
|------|------|
| 点击卡片 | 卡片瞬间缩小至 `scale(0.97)`（100ms），然后恢复并触发页面过渡 |
| 下拉刷新 | 顶部出现荧光蓝细线（2px 高），随下拉距离延伸，松手后旋转加载 |
| 滚动到底部 | 「发现更多不可思议...」文字 + 24px 荧光蓝旋转圆环，`padding: 32px 0` |
| 滚动到底终止 | 「— 已展示全部旅行体验 —」，12px `#666666` 居中，`padding: 32px 0 64px` |
| 无网络 | 见第 8 章特殊状态设计 |

---

## 7. 响应式设计规范

### 7.1 断点定义

| 断点 | 范围 | 设备类别 |
|------|------|----------|
| **Mobile** | 320px – 767px | 智能手机（竖屏） |
| **Tablet** | 768px – 1199px | 平板 / 折叠屏展开态 |
| **Desktop** | 1200px+ | 桌面浏览器 |

### 7.2 各端布局参数

| 属性 | Mobile | Tablet | Desktop |
|------|--------|--------|---------|
| 瀑布流列数 | **1 列** | **2 列** | **3 列** |
| 页面边距 | 24px | 48px | `max(80px, calc((100vw - 1440px) / 2))` |
| 卡片间距（垂直） | 24px | 32px | 32px |
| 卡片间距（水平） | N/A | 16px | 24px |
| 最大内容宽度 | 100% | 100% | 1440px（居中） |
| H1 字号 | 32px | 48px | 64px |
| H2 字号 | 22px | 24px | 28px |
| Body 字号 | 16px | 16px | 17px |
| 筛选栏 | 横向滚动（`overflow-x: auto`） | 横向排列 | 横向排列 |
| 详情页轮播高度 | 60vh（min 360px） | 70vh（min 480px） | 70vh（min 640px, max 800px） |
| 详情页正文宽度 | 100% | 100% | `max-width: 600px`（居中偏左） |
| 导航栏高度 | 56px | 64px | 72px |

### 7.3 安全区适配

```css
/* iPhone X+ 安全区 */
.page {
  padding-left: max(24px, env(safe-area-inset-left));
  padding-right: max(24px, env(safe-area-inset-right));
}

.top-bar {
  padding-top: max(0px, env(safe-area-inset-top));
}

.scroll-bottom {
  padding-bottom: max(64px, env(safe-area-inset-bottom));
}
```

### 7.4 图片响应式

```html
<!-- 封面图：根据列宽度加载不同分辨率 -->
<img srcset="
  /covers/baikal-ice-dive-750w.webp  750w,
  /covers/baikal-ice-dive-1080w.webp 1080w,
  /covers/baikal-ice-dive-1500w.webp 1500w
"
sizes="(max-width: 767px) 100vw, (max-width: 1199px) 50vw, 33vw"
src="/covers/baikal-ice-dive-1080w.webp"
loading="lazy"
decoding="async"
/>
```

---

## 8. 特殊状态设计

### 8.1 加载状态

| 场景 | 样式 | 参数 |
|------|------|------|
| **首页首次加载** | 全屏骨架屏（`#141414` 背景上的 `#1a1a1a` 矩形占位块），微弱呼吸动画 | 骨架屏 `animation: skeleton-breath 2s ease-in-out infinite;` 亮度在 `#1a1a1a` ↔ `#222222` 之间交替 |
| **瀑布流加载更多** | 荧光蓝旋转圆环（24px）+ 文字「发现更多不可思议...」 | 圆环 `border: 2px solid rgba(0,240,255,0.2); border-top-color: #00f0ff;` |
| **图片加载中** | 低分辨率占位 → 模糊 → 高清过渡 | 见 5.3 |
| **搜索联想加载** | 联想区域中央 20px 荧光蓝旋转圆环 | 与上方同风格，缩小版 |
| **详情页首次加载** | 媒体区 `#141414` 占位（70vh）+ 下方模块骨架屏 | 骨架屏宽度随机 60%-90%，模拟真实内容布局 |

**骨架屏呼吸动画**：
```css
@keyframes skeleton-breath {
  0%, 100% { background-color: #1a1a1a; }
  50% { background-color: #222222; }
}
```

### 8.2 空状态

| 场景 | 图标 | 标题 | 副标题 | 操作 |
|------|------|------|--------|------|
| **筛选无结果** | 72px 线条风格插画（荧光蓝描边指南针/地图图标） | 「没有找到匹配的旅行体验」（16px, `#a0a0a0`） | 「试试调整筛选条件」（14px, `#666666`） | 「清除筛选」次要按钮 |
| **搜索无结果** | 同上 | 「没有找到与"{关键词}"相关的旅行体验」（16px, `#a0a0a0`） | 「试试其他关键词」（14px, `#666666`） | 下方展示 5 个热门搜索词标签 |
| **内容失效** | 同上 | 「该旅行内容暂不可用」（16px, `#a0a0a0`） | — | 「返回首页」次要按钮 |

**布局**：垂直居中，图标→标题（间距 24px）→副标题（间距 8px）→操作按钮（间距 24px）

### 8.3 错误状态

| 场景 | 图标 | 标题 | 操作 |
|------|------|------|------|
| **网络错误** | 72px 线条风格断网图标（`#ff4444` 描边） | 「网络连接失败」（16px, `#a0a0a0`） | 「点击重试」主按钮（`#ff4444` 背景） |
| **加载超时**（>10s） | 同上 | 「加载超时」（16px, `#a0a0a0`）+「请检查网络后重试」（14px, `#666666`） | 「重新加载」次要按钮 |
| **图片加载失败**（单张） | `#141414` 灰色背景 + 24px 裂图图标（`#666666` 居中） | 无文字 | 不影响同卡片其他图片和文字 |

---

## 9. 核心页面布局文字描述

### 9.1 首页

**页面状态**：首次打开，从顶部开始描述。

```
┌──────────────────────────────────┐
│  [品牌标识]           [🔍 搜索] │  ← 顶栏 56px，背景透明→滚动后半透明黑
├──────────────────────────────────┤     backdrop-filter: blur(20px)
│  体验类型 │ 视觉风格 │ 小众程度 │  ← 筛选栏（吸顶），48px 高
│  [水下][高空][极地]...  [重置]  │     未展开时每个维度仅显示前 3 个标签
├──────────────────────────────────┤     +「...」展开按钮
│                                  │
│  ┌──────────────────────────┐   │
│  │                      ┌──┐│   │  ← 小众程度角标（左上角）
│  │                      │冷││   │
│  │     [封面图片]       │门││   │     图片区域占卡片 70%
│  │                      └──┘│   │     无文字侵入上半部分
│  │  ░░░░░░░░░░░░░░░░░░░░░░ │   │  ← 渐变遮罩（底部 120px）
│  │  西伯利亚冰潜：贝加尔   │   │  ← 标题 24px Bold 白色 line-clamp:2
│  │  湖零下40度的蓝          │   │
│  │  在世界上最深的淡水湖... │   │  ← 描述 14px #a0a0a0 line-clamp:1
│  │  [水下] [极地]           │   │  ← 标签 12px #00f0ff
│  │  贝加尔湖 · 5天4晚       │   │  ← 辅助信息 12px #666666
│  └──────────────────────────┘   │
│              ← 24px →            │  ← 卡片间距（手机单列 24px）
│  ┌──────────────────────────┐   │
│  │       [下一张卡片]       │   │
│  └──────────────────────────┘   │
│                                  │
│          ◌ 发现更多不可思议...   │  ← 加载指示器（触底时显示）
│                                  │
│    — 已展示全部旅行体验 —        │  ← 终止提示（全部加载完毕）
│                                  │
│       ← 64px 底部留白 →          │
└──────────────────────────────────┘
```

**视觉层次分析**：
1. **第一眼**：被高亮封面图吸引（暗色背景中图片是唯一大面积亮色区域）
2. **扫视**：小众程度角标传递「稀缺性」信号（荧光蓝 = 冷门）
3. **决策**：标题确认兴趣 → 描述判断匹配度 → 标签验证类型
4. **滚动**：无限瀑布流触发「刷不完」的探索感

**筛选激活态**：当用户选中筛选条件后，筛选栏下方出现一行提示文字（14px `#a0a0a0`）：「已筛选 X 个条件 · 共 N 个结果」，文字右侧附带「清除筛选」文字按钮（`#00f0ff`）。瀑布流从顶部重新加载，卡片以 60ms stagger 依次滑入。

### 9.2 详情页

**页面状态**：从首页卡片点击进入，页面从右滑入。

```
┌──────────────────────────────────┐
│  [← 返回]                    [⋮]│  ← 顶栏半透明叠加在媒体区上
├──────────────────────────────────┤     position: absolute; z-index: 10
│                                  │
│    ┌────────────────────────┐    │
│    │                        │    │  ← 媒体轮播区
│    │    [图片/视频轮播]     │    │     70vh 高，全屏宽
│    │    左右滑动切换        │    │     无指示器无控制按钮
│    │                        │    │     图片支持双指缩放
│    └────────────────────────┘    │
│                                  │
│  ← 32px 模块间距 →              │
│                                  │
│  📍 贝加尔湖                     │  ← 目的地（24px Bold 白色）
│     俄罗斯西伯利亚               │  ← 所属地区（14px #a0a0a0）
│                                  │
│  ┌──────────┬──────────┐        │
│  │ ⏱ 5天4晚 │ 🌡 2月-3月│       │  ← 核心信息卡片组
│  ├──────────┼──────────┤        │     2×2 网格，8px 间距
│  │ 📊 硬核   │          │        │     每个单元 #141414 背景
│  └──────────┴──────────┘        │     border-radius: 8px
│                                  │     padding: 16px
│  ← 32px →                       │
│                                  │
│  西伯利亚冰潜：贝加尔湖         │  ← 标题（32px Bold 白色）
│  零下40度的蓝                    │
│  [水下] [极地] [极简] [自然光]  │  ← 标签组（12px #00f0ff）
│  ┌──┐                            │
│  │冷│ ← 小众程度角标             │
│  └──┘                            │
│                                  │
│  ← 48px 模块间距（故事区前大留白）→│
│                                  │
│  冰层之下：一个关于勇气的故事    │  ← 故事标题（24px SemiBold）
│                                  │
│  · 在晶莹剔透的冰层下体验...     │  ← 故事亮点（14px #a0a0a0）
│  · 世界最深淡水湖的能见度...     │     项目符号列表
│  · 专业冰潜教练1对1陪同...      │     间距 8px
│                                  │
│  我从未想过自己会在西伯利亚     │  ← 故事正文（16px Regular）
│  的冬天，站在一米厚的冰层上     │     max-width: 600px
│  ...                            │     段落间距 24px
│                                  │
│  ← 32px →                       │
│                                  │
│  ┌──┬──┬──┐                     │
│  │图│图│图│                     │  ← 高清图集（3 列网格）
│  │  │  │  │                     │     间距 8px
│  ├──┼──┼──┤                     │     点击进入大图浏览
│  │图│图│图│                     │
│  └──┴──┴──┘                     │
│                                  │
│  ← 48px →                       │
│                                  │
│  同风格体验                      │  ← 推荐逻辑标签（14px #a0a0a0）
│  ┌──────────┐  ┌──────────┐    │
│  │推荐卡片1 │  │推荐卡片2 │    │  ← 相关推荐（横向滚动）
│  └──────────┘  └──────────┘    │     卡片宽度 280px
│                                  │
│       ← 64px 底部留白 →          │
└──────────────────────────────────┘
```

**「实用信息优先」的视觉实现**：
媒体轮播结束后，用户首先看到的是核心信息四宫格（目的地+地区+时长+季节+难度），而非标题。这通过以下方式强化：
- 核心信息区使用 `#141414` 填充的卡片背景，与深黑主背景形成微弱层次区分
- 核心信息图标使用 `#00f0ff` 线条图标（20px），统一风格
- 标题区在核心信息区之后，形成「先判断能不能去，再看是什么体验」的浏览动线

### 9.3 筛选结果页

**页面状态**：用户在首页选中筛选条件后的即时反馈视图。注意：这不是独立页面，而是首页筛选激活后的视觉变化。

```
┌──────────────────────────────────┐
│  [品牌标识]           [🔍 搜索] │  ← 顶栏（不变）
├──────────────────────────────────┤
│  ●体验类型 │ 视觉风格 │ 小众程度│  ← 筛选栏：已选维度前出现蓝色圆点
│  [水下●][高空][极地]...  [重置] │     「水下」为选中态（荧光蓝填充）
│  已筛选 3 个条件 · 共 2 个结果  │     ← 状态提示行（新增）
│  清除筛选                        │     ← 清除按钮
├──────────────────────────────────┤
│                                  │
│  ┌──────────────────────────┐   │
│  │    [筛选结果卡片 1]      │   │  ← 仅展示匹配的结果
│  └──────────────────────────┘   │     卡片以 stagger 60ms 依次滑入
│              ← 24px →            │
│  ┌──────────────────────────┐   │
│  │    [筛选结果卡片 2]      │   │
│  └──────────────────────────┘   │
│              ← 24px →            │
│                                  │
│    — 已展示全部 2 个结果 —       │  ← 终止提示（非「全部旅行体验」）
│                                  │
│       ← 64px 底部留白 →          │
└──────────────────────────────────┘

若筛选结果为空：

┌──────────────────────────────────┐
│            （空状态）             │
│                                  │
│           ◯ 指南针图标           │  ← 72px 荧光蓝线条插画
│                                  │
│     没有找到匹配的旅行体验       │  ← 16px #a0a0a0
│       试试调整筛选条件           │  ← 14px #666666
│                                  │
│        ┌──────────────┐          │
│        │   清除筛选   │          │  ← 次要按钮（白色边框）
│        └──────────────┘          │
│                                  │
└──────────────────────────────────┘
```

**筛选反馈时序**：
1. `t=0ms`：用户点击筛选标签
2. `t=0-100ms`：标签变为选中态（荧光蓝填充动画 100ms）
3. `t=0-200ms`：当前卡片列表 `opacity: 0;` 淡出
4. `t=200-300ms`：筛选状态提示行出现
5. `t=300-600ms`：新结果卡片以 stagger 60ms 依次滑入（`translateY(20px)→0; opacity: 0→1`）
6. 总过渡时间：≤ 600ms

---

## 10. 品牌视觉延伸

### 10.1 应用图标

```
┌──────────────────────┐
│    #0a0a0a 圆角方形  │  ← 背景
│                      │
│       ◯  地球        │  ← 荧光蓝 (#00f0ff) 极简线条地球
│      / \             │     线宽 2px，不闭合（开口设计）
│     /   \            │
│                      │     地球右下角叠加小三角形
│        ▷              │     （播放/探索符号），#00f0ff 填充
│                      │
└──────────────────────┘
```

- 形状：iOS 圆角方形（`border-radius: 22px`），无边框
- 背景：纯黑 `#0a0a0a`
- 图形：极简线条风地球 + 探索三角，荧光蓝 `#00f0ff`，线宽 2px
- 含义：「在地球上探索未知」—— 线条不闭合暗示「发现永无止境」
- 禁止：渐变背景、文字、复杂图形

### 10.2 启动屏

- 背景：`#0a0a0a` 纯黑
- 中央：品牌标识的简化版（仅地球线条 + 探索三角），荧光蓝 `#00f0ff`，尺寸 64×64px
- 底部：极细荧光蓝线（1px 高，`opacity: 0.3`），随加载进度从左到右填充
- 无文字（品牌名在加载完成后首页顶栏展示）
- 启动屏持续 ≥ 1.5s（即使用户设备极快加载，也保留最小展示时间以建立品牌认知）

### 10.3 空状态插画风格

- 风格：**极简线条风（Line Art）**
- 描边色：荧光蓝 `#00f0ff` 为主，`#a0a0a0` 为辅
- 线宽：1.5px-2px
- 背景：透明（直接画在 `#0a0a0a` 上）
- 禁止：填充色块、渐变、3D 效果、卡通形象

### 10.4 Favicon / 网页图标

- 16×16px / 32×32px 极简地球线条（与 App 图标相同图形，去除细节）
- 或直接使用荧光蓝色 `#00f0ff` 的 2px 线条圆圈 + 箭头（最简方案）

---

## 11. 设计差异化总结

### 差异化特点一：暗黑沉浸式图片剧场（Dark Immersive Image Theater）

> **核心理念**：不是「在页面上放图片」，而是「在黑暗中打开一扇扇通向未知世界的窗」。

| 维度 | 本设计 | 传统旅行 App |
|------|--------|-------------|
| 背景色 | 深黑 `#0a0a0a` | 白色/浅灰 |
| 图片地位 | 绝对主角，占据 70%+ 视觉面积 | 配图/缩略图，多为方格排列 |
| 品牌色 | 单一荧光蓝 `#00f0ff` | 多色系（红橙黄绿） |
| 空间感 | 大量留白 + 无边框设计 | 卡片分割、间距紧凑 |
| 用户心理 | 「我在探索秘密」 | 「我在浏览商品」 |

**实现方式**：
- 深黑背景使图片成为页面唯一大面积亮色区域，形成自然的视觉焦点
- 无阴影（纯暗黑风格通过边框和透明度区分层次，而非投影）
- 筛选栏和导航栏使用毛玻璃材质（`backdrop-filter: blur(20px)`），悬浮于内容之上却不阻断视觉流
- 每屏只展示 1-2 张卡片（手机单列），避免多图并排分散注意力

### 差异化特点二：反商业化情绪设计（Anti-Commercial Emotional Design）

> **核心理念**：不是「卖出更多旅行产品」，而是「激发探索未知的渴望」。

| 维度 | 本设计 | 传统旅行 App |
|------|--------|-------------|
| 色彩情绪 | 神秘、冷静、未来感 | 温暖、热闹、促销感 |
| CTA 按钮 | 仅「重试」「清除筛选」等工具性按钮 | 「立即预订」「限时抢购」 |
| 信息层级 | 图片 → 故事 → 实用信息（无价格） | 价格 → 评分 → 图片 |
| 社交元素 | 无（点赞/评论/分享均不做） | 评论数、星级、达人推荐 |
| 紧迫感 | 无倒计时、无库存提示、无秒杀 | 倒计时、「仅剩X间」 |

**实现方式**：
- 整个页面无价格信息、无评分星级、无用户头像——只有内容和探索
- 品牌色仅用于功能强调（已选标签、可点击元素），不用作装饰
- 故事叙述区前设置 48px 大留白（见 9.2 详情页布局），制造「翻页进入新章节」的仪式感
- 相关推荐命名为「同风格体验」「同类型体验」（而非「猜你喜欢」「买了还买」），避免电商语境

### 差异化特点三：三维审美筛选（Aesthetic-Driven Discovery）

> **核心理念**：用户不只按「去哪儿」筛选，而是按「这是什么感觉」筛选。

| 维度 | 本设计 | 传统旅行 App |
|------|--------|-------------|
| 筛选维度 | 体验类型 × 视觉风格 × 小众程度 | 目的地 × 价格 × 星级 |
| 筛选交互 | 标签点选，实时刷新，无需确认 | 下拉菜单 + 滑块 + 「确定」按钮 |
| 视觉反馈 | 选中标签变为荧光蓝填充，选中维度有蓝色圆点标记 | 筛选条件显示在顶部或侧边栏 |
| 筛选引导 | 筛选栏常驻首页顶部（吸顶），主动邀请探索 | 筛选通常隐藏在角落图标中 |

**实现方式**：
- 筛选维度从传统旅行的「功能性维度」（价格/评分/距离）转变为「审美性维度」（体验类型/视觉风格/小众程度）
- 筛选按钮默认展示部分选项，降低初始视觉噪音，点击展开完整列表（300ms 过渡）
- 组合筛选结果取交集，越筛选越精准——每次筛选都是对用户审美偏好的进一步聚焦
- 空状态引导文案为「试试调整筛选条件」（而非「没有找到产品」），鼓励探索而非放弃

---

## 附录 A：CSS 自定义属性（Design Tokens）

```css
:root {
  /* === 颜色 === */
  --color-bg-primary: #0a0a0a;
  --color-bg-card: #141414;
  --color-bg-skeleton: #1a1a1a;
  --color-text-primary: #ffffff;
  --color-text-secondary: #a0a0a0;
  --color-text-tertiary: #666666;
  --color-brand: #00f0ff;
  --color-brand-dark: #00d8e5;
  --color-brand-gradient: linear-gradient(135deg, #00f0ff, #0080ff);
  --color-brand-glow: rgba(0, 240, 255, 0.10);
  --color-brand-glow-strong: rgba(0, 240, 255, 0.20);
  --color-error: #ff4444;
  --color-success: #00ff88;
  --color-warning: #ffaa00;
  --color-border-default: rgba(255, 255, 255, 0.08);
  --color-border-hover: rgba(255, 255, 255, 0.20);
  --color-border-active: #00f0ff;

  /* === 排版 === */
  --font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  --font-mono: 'JetBrains Mono', 'SF Mono', monospace;
  --text-h1-size: 48px;
  --text-h1-weight: 700;
  --text-h1-line-height: 1.1;
  --text-h1-letter-spacing: -0.02em;
  --text-h2-size: 24px;
  --text-h2-weight: 600;
  --text-h2-line-height: 1.2;
  --text-h2-letter-spacing: -0.01em;
  --text-body-size: 16px;
  --text-body-weight: 400;
  --text-body-line-height: 1.5;
  --text-small-size: 14px;
  --text-small-weight: 400;
  --text-small-line-height: 1.4;
  --text-caption-size: 12px;
  --text-caption-weight: 500;
  --text-caption-line-height: 1.3;

  /* === 间距 === */
  --space-xs: 8px;
  --space-sm: 16px;
  --space-md: 24px;
  --space-lg: 32px;
  --space-xl: 48px;
  --space-2xl: 64px;
  --space-3xl: 80px;

  /* === 圆角 === */
  --radius-card: 12px;
  --radius-button: 8px;
  --radius-tag: 20px;
  --radius-small: 4px;
  --radius-full: 50%;

  /* === 动画 === */
  --transition-base: 300ms cubic-bezier(0.33, 1, 0.68, 1);
  --transition-fast: 150ms cubic-bezier(0.33, 1, 0.68, 1);
  --transition-slow: 500ms cubic-bezier(0.33, 1, 0.68, 1);
  --transition-image: 500ms ease-out;

  /* === 断点 === */
  --bp-mobile: 767px;
  --bp-tablet: 768px;
  --bp-desktop: 1200px;

  /* === 层级 === */
  --z-base: 1;
  --z-sticky: 10;
  --z-overlay: 20;
  --z-modal: 30;
  --z-toast: 40;
}
```

---

## 附录 B：设计验收检查清单

| 序号 | 检查项 | 标准 |
|------|--------|------|
| 1 | 所有页面背景色为 `#0a0a0a` | 无例外 |
| 2 | 品牌色仅以点状出现，每个视口 ≤3 处 | 按钮、选中标签、链接 |
| 3 | 所有图片上方 60% 区域无文字覆盖 | 仅底部渐变遮罩区域可有文字 |
| 4 | 文字对比度满足 WCAG AA+ | 主文字 21:1，次文字 7.9:1 |
| 5 | 所有间距为 8 的整数倍 | 无 5px/7px/13px 等数值 |
| 6 | 动画时长统一 300ms（基础） | 无 200ms/250ms/350ms 混用 |
| 7 | 卡片无阴影 | `box-shadow: none` |
| 8 | 悬停效果仅在桌面端生效 | `@media (hover: hover)` |
| 9 | 所有可交互元素触摸区域 ≥44×44pt | 按钮高度 48px 或外围补足 |
| 10 | 三端断点下布局正确 | Mobile 1 列 / Tablet 2 列 / Desktop 3 列 |

---

*文档结束*
